import type { LatLngLiteral } from "@/types/navigation";

export async function getMapboxRoute(coords: LatLngLiteral[], options?: any): Promise<any> {
  // Stub
  return { steps: [], totalDistance: 0, totalDuration: 0 };
}
